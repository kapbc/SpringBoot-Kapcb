<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kapcb.ccc.mapper.UserMapper">

    <resultMap id="demo" type="User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="created" column="created"/>
        <result property="updated" column="updated"/>
    </resultMap>

    <select id="findAllUser" resultMap="demo">
        SELECT * FROM tb_user LIMIT #{currentIndex},#{pageSize}
    </select>

    <select id="findUserByQuery" resultMap="demo">
        SELECT * FROM tb_user
        WHERE 1=1
        <if test="username != null and username != ''">
            and username LIKE CONCAT('%',#{username},'%')
        </if>
        <if test="email != null and email != ''">
            and email LIKE CONCAT('%',#{email},'%')
        </if>
    </select>

    <select id="findUserByBatch" resultMap="demo">
        SELECT * FROM tb_user
        WHERE 1=1
        <if test="username != null and username != ''">
            and username LIKE CONCAT('%',#{username},'%')
        </if>
        <if test="email != null and email != ''">
            and email LIKE CONCAT('%',#{email},'%')
        </if>
    </select>

    <insert id="insertUser">
        INSERT INTO tb_user (username,password,phone,email,created,updated)
        VALUES (#{username},#{password},#{phone},#{email},#{created},#{updated})
    </insert>

    <update id="updateUser">
        UPDATE tb_user
        <set>
            <if test="username != null">username = #{username}</if>
            <if test="password != null">password = #{password}</if>
            <if test="phone != null">phone = #{phone}</if>
            <if test="email != null">email = #{email}</if>
            <if test="created != null">created = #{created}</if>
            <if test="updated != null">updated = #{updated}</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteUser">
        DELETE FROM tb_user WHERE id IN
        <foreach collection="array" item="ids" index="index" open="(" separator="," close=")">
            #{ids}
        </foreach>
    </delete>

</mapper>